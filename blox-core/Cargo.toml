[package]
name = "blox-core"
version = "0.1.1"
edition.workspace = true
license.workspace = true
description = "Pure, deterministic, I/O-free order book and matching engine."
repository = "https://github.com/algosolltd/blox"

# No runtime dependencies beyond std. See ../docs/CORE_API.md.
[dependencies]

[dev-dependencies]
